<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ItomoriLog.UI.ViewModels"
             x:Class="ItomoriLog.UI.Views.LogGridView"
             x:DataType="vm:LogsPageViewModel">

  <Grid RowDefinitions="Auto,*,Auto">
    <!-- Filter Bar -->
    <Border Grid.Row="0" Background="{StaticResource Itomori.Panel}" Padding="10,6">
      <StackPanel Orientation="Horizontal" Spacing="8">
        <TextBox Watermark="Text search..."
                 Name="FilterTextBox"
                 Text="{Binding TextSearch}"
                 Width="190"
                 FontSize="12" />
        <TextBox Watermark="Query (text + timestamp IN '$start..$latest')"
                  Name="QueryTextBox"
                  Text="{Binding QueryText}"
                  Width="380"
                  FontSize="12" />
        <Button Content="↻ Refresh" Command="{Binding RefreshCommand}" />
        <TextBlock Text="{Binding StatusText}"
                     Foreground="{StaticResource Itomori.SubtleForeground}"
                    VerticalAlignment="Center" Margin="8,0,0,0"
                    FontSize="11" />
        <TextBlock Text="{Binding QueryParseError}"
                    Foreground="{StaticResource Brush.Error}"
                    VerticalAlignment="Center"
                    Margin="8,0,0,0"
                    FontSize="11"
                    IsVisible="{Binding QueryParseError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
      </StackPanel>
    </Border>

    <!-- Log DataGrid -->
    <DataGrid Grid.Row="1"
              Name="LogDataGrid"
              ItemsSource="{Binding CurrentPage}"
              SelectedItem="{Binding SelectedRow}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserReorderColumns="True"
              CanUserResizeColumns="True"
              CanUserSortColumns="False"
              GridLinesVisibility="Horizontal"
              RowHeight="22"
              ColumnHeaderHeight="24"
              HeadersVisibility="Column"
              BorderThickness="0"
              RowBackground="#141922"
              Background="{StaticResource Itomori.Background}"
              Foreground="{StaticResource Itomori.Foreground}">
      <DataGrid.Styles>
        <Style Selector="DataGridRow">
          <Setter Property="Foreground" Value="{StaticResource Itomori.Foreground}" />
          <Setter Property="MinHeight" Value="22" />
        </Style>
        <Style Selector="DataGridCell">
          <Setter Property="Foreground" Value="{StaticResource Itomori.Foreground}" />
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="Padding" Value="8,2" />
          <Setter Property="FontSize" Value="12" />
        </Style>
        <Style Selector="DataGridColumnHeader">
          <Setter Property="Padding" Value="8,4" />
          <Setter Property="FontSize" Value="11" />
        </Style>
      </DataGrid.Styles>
      <DataGrid.Columns>
        <DataGridTemplateColumn Header="Timestamp" Width="210">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate x:DataType="vm:LogRowDto">
              <TextBlock Text="{Binding Timestamp}"
                          Foreground="{StaticResource Itomori.Foreground}"
                         VerticalAlignment="Center"
                         FontSize="12" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="Level" Width="72">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate x:DataType="vm:LogRowDto">
              <Border CornerRadius="3" Padding="4,1" HorizontalAlignment="Left"
                       VerticalAlignment="Center">
                <TextBlock Text="{Binding Level}"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Itomori.Foreground}" />
              </Border>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="Source" Width="150">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate x:DataType="vm:LogRowDto">
              <TextBlock Text="{Binding Source}"
                          Foreground="{StaticResource Itomori.Foreground}"
                          VerticalAlignment="Center"
                          FontSize="12"
                          TextTrimming="CharacterEllipsis" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="Message" Width="*">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate x:DataType="vm:LogRowDto">
              <TextBlock Text="{Binding Message}"
                          Foreground="{StaticResource Itomori.Foreground}"
                          VerticalAlignment="Center"
                          FontSize="12"
                          TextTrimming="CharacterEllipsis" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>

    <!-- Browse Status -->
    <Border Grid.Row="2" Background="{StaticResource Itomori.Panel}" Padding="10,4">
      <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
        <TextBlock Text="{Binding StatusText}"
                    Foreground="{StaticResource Itomori.Foreground}"
                     VerticalAlignment="Center"
                    FontSize="11" />
        <TextBlock Text="Scroll for more"
                    Foreground="{StaticResource Itomori.SubtleForeground}"
                    VerticalAlignment="Center"
                    FontSize="11"
                    IsVisible="{Binding HasNextPage}" />
        <Button Content="Detail" Command="{Binding ToggleDetailCommand}" Margin="20,0,0,0" />
      </StackPanel>
    </Border>
  </Grid>

</UserControl>
